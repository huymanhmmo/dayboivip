---
const currentYear = new Date().getFullYear();
import CourseLayout from '../../../layouts/CourseLayout.astro';
import { adults } from '../../../data/coursesEn.js';

const breadcrumbs = [
  { label: 'Home', href: '/en/' },
  { label: 'Swimming Courses', href: '/en/courses/' },
  { label: 'Adult Swimming Lessons' },
];

import { generateCourseSchema } from '../../../utils/courseSchema.js';
const courseSchema = generateCourseSchema(adults, 'https://dayboi.vip/en/courses/adults/');

const combinedSchema = [courseSchema];
---

<CourseLayout
  course={adults}
  breadcrumbs={breadcrumbs}
  schema={combinedSchema}
  lang="en"
/>
